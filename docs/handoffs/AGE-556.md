# AGE-556 Handoff

## Status

Linear issue AGE-556 was created for the next slice:
`[08/08] Sell fills, buying power, and reinvest cash plan`.

The issue is in Backlog under Swing Command Center and is related to AGE-531.

Current main should already include the cleanup that makes this issue easier:

- holdings can be added/updated/removed in the local session
- added holdings become editable research cards
- journal shows a first-pass profit cash plan after reserve and pay-yourself
- copy is more plain-English and keeps the app framed as manual planning

## Next

Start AGE-556 from synced `main`.

Implement sell fills as the source of truth for realized gain and buying power:

- add planned/ordered/filled/canceled/reviewed sell-fill statuses
- support manual sell entry and local CSV/JSON import
- calculate cash raised, cost basis removed, realized gain/loss, reserve,
  pay-yourself set-aside, and reinvestable cash from filled sells only
- add a buying-power summary
- feed remaining reinvestable cash into the profit/research planner
- export sell-fill and buying-power data in the tax-review planning JSON

## Risks

- Keep this manual only. No broker credentials, scraping, Robinhood login,
  order execution, or automated trading.
- Do not present reinvest rows as recommendations.
- Do not call tax output a filing document or tax advice.
- Missing shares, cost basis, fill price, or buying-power values should remain
  visibly missing rather than guessed.

## Files

Expected areas:

- `src/App.tsx`
- `src/App.css`
- `src/lib/tradeJournal.ts`
- new sell-fill/buying-power helper if the model grows beyond journal helpers
- relevant tests in `src/lib/*.test.ts`
- `docs/architecture.md`
- `docs/handoffs/AGE-556.md`

## Checks

Run:

- `npm run lint`
- `npm run typecheck`
- `npm test -- --run`
- `npm run build`
- browser/dev-server verification for adding/importing a sell, marking it
  filled, and seeing buying power/reinvestable cash update
